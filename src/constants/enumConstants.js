const bookingStatus = {
  paidBooked: 'paidBooked',
  trialBooked: 'trialBooked',
  confirmBooked: 'confirmBooked',
  slotOpened: 'slotOpened'
}

const taskRosterUserStatus = {
  active: 'ACTIVE',
  deactive: 'DEACTIVE'
}

module.exports = {
  bookingStatus,
  taskRosterUserStatus
}